# PostgreSQL (Railway: use the DATABASE_URL from the Postgres plugin)
DATABASE_URL="postgresql://user:password@localhost:5432/photo_delivery?schema=public"

# NextAuth — set a long random string in production
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-openssl-rand-base64-32"

# S3-compatible object storage (Cloudflare R2, AWS S3, MinIO, DigitalOcean Spaces, Supabase S3 API, etc.)
# Endpoint URL (no trailing slash), e.g. https://<accountid>.r2.cloudflarestorage.com
STORAGE_URL="https://your-endpoint.example.com"
STORAGE_BUCKET="your-bucket-name"
STORAGE_KEY="your-access-key-id"
STORAGE_SECRET="your-secret-access-key"
# Optional: default "auto" works for R2; use e.g. us-east-1 for AWS S3
STORAGE_REGION="auto"
# Set to "true" for many MinIO installs; omit or "false" for R2 / AWS virtual-hosted style
# STORAGE_FORCE_PATH_STYLE="false"
